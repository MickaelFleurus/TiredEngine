#pragma once
#include "utils/Token.h"

namespace Debug {
class COverlord {
public:
    COverlord();

    // static void AddMenu(CToken& token, IOverlordItem& item);
    void Render();

private:
};
} // namespace Debug